# Captures d'écran

Dépose ici les captures référencées dans le `README` à la racine, par exemple :

- `accueil.png` — l'écran d'accueil (choix prof / élève)
- `espace-prof.png` — le catalogue de l'espace prof
- `projection.png` — le mode projection (affichage classe)

Puis décommente les lignes `![…](docs/…)` dans le `README` principal.
